# Tender Intelligence Agent — контрольная точка 2026-08-23

## Точка продолжения
Дата: 23.08.2026
Ветка: `main`

## GitHub / локальный запуск
- GitHub-подключение подтверждено и работает: `Ju4ara-49/tender-intelligence-agent` доступен для чтения и записи.
- Последняя подтверждённая локальная команда запуска: `python -m src.main bot`.
- Telegram-бот запускается.
- Ollama/Qwen работает: `provider=ollama`, модель `qwen3:8b`, URL `http://localhost:11434`.

## Fabrikant — текущее состояние
Активен `FabrikantV2Collector` через registry. Основная проблема «Фабрикант даёт почти только цену» устранена частично: строка реестра уже даёт `customer` и `deadline`, а orchestrator не позволяет detail parser перезаписывать базовые поля.

## Последний подтверждённый прогон №057
`found=138 filtered=21 new=0 analyzed=0 notified=0 dup=2 excluded=19 details=21 failed=0`.
В логах были `customer=True` и даты deadline, но часть процедур была исторической. Это подтвердило необходимость отбрасывать старые записи до загрузки detail.

## Последнее исправление
Коммит `ed2bed6a23f572e7db53b591e0d6bececc0f5d08`:
- расширено распознавание заголовков колонок Fabrikant для названия, цены, заказчика, региона, публикации и окончания подачи;
- добавлен row-local fallback для формата `Метка: значение`, если Fabrikant меняет/объединяет заголовки таблицы;
- в `raw_data.search_row` сохраняются фактические `headers`, `values`, mapping индексов и исходные строки дат;
- lookback по `published_at` применяется уже внутри Fabrikant search, до дорогой загрузки detail;
- detail остаётся дополнительным источником и не должен заменять поля search row в orchestrator.

## Важное наблюдение по реальному тендеру
Для закупки `0301300051525000248` внешняя карточка подтверждает, что корректные поля существуют: опубликовано 09.07.2025, окончание подачи 17.07.2025 06:00 МСК, регион Башкортостан/Уфа, заказчик ГБУЗ РБ ГКБ №21. Поэтому задача — правильно забрать эти поля из Fabrikant, а не придумывать их через AI.

## Следующий обязательный тест
```powershell
cd C:\Users\Ju4ara\Projects\tender-intelligence-agent
git pull
python -m src.main bot
```

После запуска выполнить новый поиск с включённым Fabrikant.

Проверить:
1. Старые процедуры не должны массово доходить до detail.
2. Для свежих процедур лог должен содержать `customer=True` и актуальный `deadline`.
3. Telegram и Excel должны показывать дату закупки/публикации, заказчика, регион и срок подачи, когда эти поля есть в строке реестра.
4. `raw_data.search_row.mapping` должен позволять быстро определить ошибку конкретной колонки, если поле снова окажется пустым.
5. Не менять Telegram, Excel, EIS/B2B и Ollama без отдельной необходимости.

## Контрольные коммиты
- `3c145aa` — переход к Fabrikant V2 и checkpoint.
- `480dcbb181efeda6dd85ccc165d516a1e4b52fef` — защита search-row полей от detail overwrite.
- `1c736c89014845304473ab85c651eeed11f5b608` — lookback до detail loading.
- `ed2bed6a23f572e7db53b591e0d6bececc0f5d08` — усиление mapping полей и lookback в Fabrikant V2.
