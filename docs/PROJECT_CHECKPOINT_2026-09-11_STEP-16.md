# Контрольная точка — 2026-09-11 — STEP-16

## Состояние остановки
Зафиксирован новый регрессионный тест для legacy notification bridge. Тест намеренно должен показать текущий дефект: если старое уведомление уже существует, а тендер затем изменился, `NotificationDeliveryState` может принять текущий изменённый state за уже отправленный baseline.

## HEAD
`b5c6f0c9bff6405631e4641749c1736cdaf6b615`

## Что добавлено
`tests/test_notification_delivery.py` проверяет сценарий: отправка → изменение НМЦК → сохранение → изменённый тендер обязан считаться новым событием.

## Ограничение
Попытка сразу заменить реализацию `src/storage/notification_delivery.py` через GitHub write API была отклонена системой безопасности инструмента. Поэтому код пока не изменён, а регрессионный тест сохранён отдельно, чтобы дефект не потерялся.

## Следующий шаг
Проверить CI. Затем продолжить исправление notification bridge разрешённым способом. До зелёного CI не считать блок завершённым.
