# Контрольная точка — 2026-09-12 — STEP-21

## Состояние остановки
Работа остановлена для фиксации промежуточного состояния независимо от результата текущего CI.

## Последние изменения
1. Добавлено кросс-платформенное извлечение коммерческих условий непосредственно в `Tender.__post_init__`:
   - аванс (%);
   - постоплата/отсрочка (дни);
   - обеспечение заявки (%);
   - обеспечение контракта (%).
   Уже явно распарсенные значения не перезаписываются.
2. Добавлен `tests/test_tender_commercial_terms.py`.
3. Добавлен тест, что изменение `start_date` создаёт новый notification event.

## CI
Для commit `316f248549aea38d2d330c35b8d0c2aa5e731695` запущен полный CI run `34649606382`; на момент checkpoint он ещё выполняется на ранней стадии setup Python.

## Следующий шаг
Сначала проверить итог CI. При ошибке — checkpoint → исправление → повтор CI. При зелёном CI — продолжить аудит customer INN, документов/ссылок и платформенных detail-полей, затем проверить Excel и полный pipeline.
