# Контрольная точка — 2026-09-11 — STEP-06

HEAD: `457c7aeb77efdb8758191266680dfbbb21e37f84`

## Исправлено
- `Tender.__post_init__()` теперь централизованно нормализует `start_date`, `end_date`, `published_at`, `deadline`.
- Naive даты от российских публичных площадок считаются MSK (UTC+3), затем переводятся в UTC.
- Уже timezone-aware даты сохраняют свой момент времени и приводятся к UTC.

Это устраняет зависимость фильтров `since`/deadline от часового пояса runner/Windows-хоста.

## Тест
Добавлен `tests/test_tender_datetime_contract.py`.

## Следующий шаг
Проверить CI после цепочки изменений. Если зелёный — продолжить расширение collector contract tests и устойчивость к частично заполненным страницам.
