# Контрольная точка проекта — 11.09.2026

## Состояние

Текущая контрольная точка сохранена в `main` после успешного CI.

HEAD на момент создания контрольной точки: `b398f13066c2afaa0cd1f802f44051bea0232086`.

## Что уже исправлено

- Добавлен рабочий `src/ai/analyzer.py` для локального Ollama.
- AI использует `http://localhost:11434`, модель `qwen3:8b`, `/api/chat`; OpenAI API не используется.
- Добавлены понятные ошибки для недоступного Ollama, timeout, HTTP 404 и некорректного JSON.
- Добавлен `truststore` в зависимости.
- Исправлен `.gitignore`: `analyzer.py` больше не исключается, удалены мусорные записи.
- Добавлены тесты AI-анализатора.
- Оркестратор реально применяет бизнес-критерии: цена, аванс, постоплата, обеспечение заявки, обеспечение контракта, срок до дедлайна.
- Исправлена семантика отсутствующих значений обеспечения/постоплаты при заданном максимуме.
- Исправлен морфологический fallback ключевых слов, чтобы не получать ложные совпадения вроде «станция» для «станок».
- Исправлен регрессионный тест B2B-Center и добавлена HTML-фикстура.
- Excel приведён к согласованной схеме из 17 колонок:
  `Площадка, Номер закупки, Наименование, Заказчик, Регион, Начальная цена, Валюта, Дата публикации, Дата окончания подачи заявок, Осталось дней до подачи, Закон, Способ закупки, AI score, Рекомендация, Краткое резюме, Риски, Ссылка`.
- Убрано старое имя UniPro из Excel mapping; текущие площадки: ЕИС, B2B-Center, РТС-тендер, Фабрикант, ТМК, Росатом.
- README синхронизирован с текущей архитектурой и локальным Ollama.
- CI переведён на детерминированные проверки без обращения к локальному Ollama.

## Последняя проверка CI

Workflow run: `34585366862`.

Результат: **success**.

Все шаги job `103218216467` прошли успешно, включая:
- deterministic tests;
- search pipeline regression tests;
- SQLite Excel integration test;
- проверку созданного Excel;
- загрузку Excel и SQLite как CI artifacts.

## Что остаётся проверить

1. Убрать/проверить стрелки автофильтра Excel — пользователь просил их не показывать.
2. Продолжить аудит Orchestrator, моделей, фильтров, Telegram, scheduler и всех collectors.
3. Проверить workflow `workflow_dispatch` для экспорта артефактов.
4. Проверить все остаточные ссылки на старые платформы/имена и потенциальные секреты.
5. Отдельно выполнить локальную проверку Ollama и реальных сетевых площадок, поскольку GitHub Runner не имеет доступа к localhost пользователя и часть внешних площадок ограничивает доступ из CI.

## Принцип работы дальше

Не считать проект готовым только потому, что CI зелёный. Продолжать аудит связки `collectors → filters → dedup/storage → AI → notifications/Telegram → Excel → scheduler` и исправлять найденные проблемы непосредственно в репозитории.
