# ТЕНДЕРБОТ — СОСТОЯНИЕ ПРОЕКТА

Дата последнего обновления: 20.08.2026
Рабочая точка: ТБ3

## 1. Название проекта

ТендерБот

Назначение:
система поиска подходящих тендеров, анализа закупок и подготовки
результатов для пользователя.

Основная коммерческая идея:
клиент получает результат работы тендерного отдела в виде Excel-файла
с отобранными и проанализированными тендерами.

Telegram используется как рабочий интерфейс настройки и управления.
Клиентский результат — Excel.

---

## 2. ТЕКУЩИЙ СТАТУС

Основная техническая цепочка работает:

ЕИС
→ поиск
→ keyword-фильтр
→ защита от дублей
→ загрузка полной карточки
→ критерии Telegram
→ SQLite
→ Ollama / Qwen3
→ AI-анализ
→ порог релевантности
→ Telegram

Дополнительно:

→ автоматический Excel-экспорт

---

## 3. ТЕКУЩАЯ ПЛОЩАДКА

Рабочий сборщик:

src/collectors/eis_zakupki.py

Площадка:

zakupki.gov.ru / ЕИС

Текущий поиск подтверждён.

Последняя проверка:

found=55
filtered=1
new=0
analyzed=0
notified=0
skipped_duplicate=1
excluded_by_criteria=0
details_loaded=0
details_failed=0

Причина:
единственный прошедший keyword-фильтр уже находился в базе
и был ранее обработан/уведомлён.

---

## 4. ЗАГРУЗКА ДЕТАЛЕЙ

Метод:

EisZakupkiCollector.get_details()

Отдельно проверен на закупке:

0163200000326004366

Результат:

TITLE:
Поставка мебели медицинской (Стол/кушетка массажный), ввод в эксплуатацию
медицинских изделий, обучение правилам эксплуатации специалистов,
эксплуатирующих медицинские изделия, и специалистов, осуществляющих
техническое обслуживание медицинских изделий

PRICE:
139860.0 RUB

RAW_DATA:

details_loaded=True
source_url=https://zakupki.gov.ru/epz/order/notice/ea20/view/common-info.html?regNumber=0163200000326004366
procurement_method=Запрос котировок в электронной форме
status=Подача заявок

Загрузка полной карточки подтверждена.

---

## 5. ЕИС-ПАРСЕР

Работает:

- поиск по ключевым словам;
- пагинация;
- дедупликация;
- извлечение регистрационного номера;
- определение 44-ФЗ / 223-ФЗ;
- извлечение названия;
- извлечение цены;
- извлечение заказчика;
- извлечение региона;
- извлечение способа закупки;
- извлечение статуса;
- извлечение даты публикации;
- извлечение начала/окончания подачи заявок;
- построение URL карточки;
- загрузка полной карточки;
- защита от CAPTCHA;
- обработка SSL через truststore.

Текущая реализация:

src/collectors/eis_zakupki.py

Парсер регистрационного номера признан рабочим.

Не менять его без фактической причины.

---

## 6. МОДЕЛЬ TENDER

Файл:

src/models/tender.py

Основные поля:

platform
external_id
title
url
description
price
currency
start_date
end_date
published_at
deadline
region
customer
law_type
raw_data

Уникальность:

platform + external_id

unique_key:

f"{platform}:{external_id}"

---

## 7. БАЗА ДАННЫХ

SQLite:

data/tenders.db

Файл:

src/storage/database.py

Есть:

- tenders;
- analyses;
- notifications;
- уникальный unique_key;
- защита от повторных уведомлений;
- обновление существующего тендера;
- сохранение AI-анализа.

---

## 8. AI

Файл:

src/ai/analyzer.py

Используется локальный Ollama.

Модель:

qwen3:8b

URL:

http://localhost:11434

Архитектура AI работает.

Ранее подтверждено:

HTTP 200
корректный JSON
stub=False
содержательный AI-анализ.

Порог отправки Telegram:

70

Не снижать порог только ради увеличения количества уведомлений.

---

## 9. TELEGRAM

Telegram используется для:

- настройки параметров поиска;
- управления ключевыми словами;
- управления критериями;
- получения уведомлений о релевантных тендерах;
- операционного управления системой.

Уведомление отправляется после:

keyword-фильтра
→ проверки дубля
→ загрузки деталей
→ критериев
→ AI-анализа
→ проверки min_ai_score.

---

## 10. EXCEL-ЭКСПОРТ

Автоматический Excel-экспорт подключён.

Файл:

src/export/excel.py

Пакет:

openpyxl 3.1.5

Результат:

output/tenders.xlsx

Экспорт автоматически выполняется после цикла поиска.

Последняя проверка Excel:

SHEETS:
Тендеры
Статистика

ROWS:
212

COLS:
21

То есть:

211 тендеров
+
1 строка заголовка.

Колонки:

1. Площадка
2. Номер закупки
3. Наименование
4. Заказчик
5. Регион
6. НМЦК
7. Валюта
8. Дата публикации
9. Дедлайн подачи
10. Дней до дедлайна
11. Закон
12. Способ закупки
13. Статус
14. AI score
15. Рекомендация
16. Краткое резюме
17. Риски
18. Бюджет
19. Комментарий по срокам
20. Ссылка
21. Дата анализа

Excel является основным коммерческим результатом системы.

---

## 11. ОРКЕСТРАТОР

Файл:

src/orchestrator.py

Последовательность:

сбор
→ keyword-фильтр
→ проверка дубля
→ загрузка деталей
→ критерии Telegram
→ сохранение в SQLite
→ AI
→ сохранение анализа
→ проверка порога
→ Telegram
→ Excel

Автоматический Excel экспорт уже подключён.

---

## 12. АКТУАЛЬНАЯ КОНФИГУРАЦИЯ

app:
  name: "Tender Intelligence Agent"
  timezone: "Europe/Moscow"

scheduler:
  interval_minutes: 60
  run_on_start: true

collectors:
  eis:
    enabled: true
    lookback_days: 3
    request_delay_seconds: 2
    max_pages: 5
    records_per_page: 10

filters:
  min_text_length: 10

ai:
  provider: "ollama"
  model: "qwen3:8b"
  ollama_url: "http://localhost:11434"
  min_relevance_score: 70
  use_stub_when_no_key: false

notifications:
  telegram:
    enabled: true
    dry_run_when_no_token: true

storage:
  database_path: "data/tenders.db"

logging:
  file: "logs/agent.log"
  max_bytes: 5242880
  backup_count: 3

---

## 13. КОММЕРЧЕСКИЕ УСЛОВИЯ

Расширенный парсинг коммерческих условий пока НЕ внедрён.

Не менять рабочий парсер ради этой функции без отдельного анализа.

Планируемые поля/критерии:

- наличие аванса;
- минимальный процент аванса;
- максимальный срок постоплаты;
- обеспечение заявки;
- диапазон обеспечения заявки;
- обеспечение контракта;
- диапазон обеспечения контракта.

Сначала необходимо определить, где эти данные стабильно находятся
в карточке ЕИС и как корректно извлекать их без ухудшения текущей работы.

---

## 14. ЧТО НЕ ДЕЛАТЬ

Не переписывать рабочий сборщик ЕИС целиком.

Не менять одновременно несколько независимых компонентов.

Не менять работающий _extract_reg_number() без фактической причины.

Не снижать AI-порог только ради большего количества Telegram-уведомлений.

Не ломать существующий Excel-экспорт при дальнейшей доработке.

Перед изменением рабочего кода сначала смотреть актуальный файл
и фактический результат выполнения.

При необходимости полной замены файла использовать PowerShell.

---

## 15. ТЕКУЩАЯ ТОЧКА РАЗРАБОТКИ

Базовая техническая цепочка подтверждена.

ЕИС:
работает.

Детали:
работают.

SQLite:
работает.

Ollama/Qwen:
работает.

AI:
работает.

Telegram:
работает.

Дубли:
работают.

Excel:
работает.

Следующий этап:

1. проверить качество AI-анализатора;
2. проверить ai_context и prompt;
3. проверить качество оценки релевантности;
4. определить, какие данные из полной карточки нужно передавать AI;
5. затем расширять бизнес-фильтры;
6. затем развивать остальные площадки.

Коммерческие условия пока не менять.

---

## 16. АКТУАЛЬНАЯ РАБОЧАЯ ТОЧКА

20.08.2026 / ТБ3

Рабочий Excel-файл:

output\tenders.xlsx

Последняя проверка:

211 тендеров
21 колонка

Рабочий тест get_details:

0163200000326004366

Результат:
details_loaded=True
price=139860.0
status=Подача заявок

Последний цикл поиска:

found=55
filtered=1
new=0
analyzed=0
notified=0
skipped_duplicate=1
excluded_by_criteria=0
details_loaded=0
details_failed=0

Причина:
результат уже присутствует в базе.

Эта точка считается стабильной.

---

## 17. B2B-CENTER — ПОДКЛЮЧЕНА

Дата обновления: 21.08.2026

Площадка B2B-Center подключена к общей архитектуре с единым
идентификатором платформы:

`b2b_center`

Подключение выполнено через:

- `src/collectors/b2b_center.py`;
- `src/collectors/registry.py`;
- `config/config.yaml`;
- Telegram `enabled_platforms`.

Подтверждено реальным HTTP-запросом без авторизации:

- поиск `подшипники` — 20 процедур;
- карточка `4571794` загружена успешно;
- получены номер, title, URL, заказчик, даты, регион, статус и
  условия оплаты (постоплата 30 календарных дней);
- цена отсутствует в самой карточке и корректно сохранена как `None`.

B2B-Center проходит проверку общего конвейера на временной SQLite БД:

collector → keyword filter → SQLite → Ollama/Qwen3 → Telegram dry-run → Excel.

Результат AI для реальной процедуры: score=85, recommendation=participate,
stub=False. Рабочая БД и существующие Excel-файлы в этой проверке не изменялись.

ЕИС повторно проверена через `get_details(0163200000326004366)`:
details_loaded успешно, price=139860.0.

Ограничение B2B-Center: публичная выдача сайта возвращает первую страницу
из 20 результатов; для запроса `page=2` сайт сейчас отдаёт тот же набор,
поэтому `max_pages` установлен в 1 и не создаёт повторов.

## 2026-08-20 — B2B-Center title parsing
- b2b_center.py компилируется без ошибок.
- B2B-Center search(['подшипники']) возвращает 20 результатов.
- Убран номер процедуры и служебный префикс из title.
- _parse_search_row восстановлен после ошибки IndentationError.
- Проверены реальные HTML-строки B2B-Center: название процедуры находится в .search-results-title-desc; внутри этого блока дополнительно присутствует подсвеченный поисковый фрагмент.
- Текущая проблема: для части процедур title всё ещё включает текст из блока подсветки/позиции и иногда служебный текст.
- Следующий шаг: аккуратно исправить извлечение именно названия процедуры из .search-results-title-desc, не ломая customer, даты, price и остальные поля.
- Последняя успешная команда: .\.venv\Scripts\python.exe -c "from src.collectors.b2b_center import B2BCenterCollector; c=B2BCenterCollector({'request_delay_seconds':0}); r=c.search(['подшипники']); print('COLLECTOR:',len(r)); [print(x.external_id,'|',x.title) for x in r]"

